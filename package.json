{
  "name": "tock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup-services": "docker-compose up -d",
    "dev:setup": "pnpm run setup-services && pnpm run -F database dev-push",
    "dev": "pnpm run dev:setup && pnpm -F graphql-api generate-schema && (trap 'kill 0' INT TERM; pnpm -r --parallel dev & pnpm -F graphql-api generate-schema:watch; wait)",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r --parallel typecheck"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
