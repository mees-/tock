[build]
builder = "RAILPACK"

[deploy]
startCommand = "pnpm --filter web preview -- --port $PORT --host 0.0.0.0"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
