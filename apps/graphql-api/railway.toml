[build]
builder = "RAILPACK"

[deploy]
startCommand = "bun apps/graphql-api/src/index.ts"
healthcheckPath = "/graphql?query=%7B__typename%7D"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
