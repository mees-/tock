{
  "name": "stripe-webhook-handler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "dev:stripe-webhook-forward": "stripe listen --forward-to localhost:4001/stripe/webhook",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.11",
    "database": "workspace:*",
    "drizzle-orm": "catalog:",
    "hono": "^4.12.3",
    "stripe": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
