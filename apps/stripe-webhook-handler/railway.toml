[build]
builder = "RAILPACK"
watchPatterns = ["apps/stripe-webhook-handler/**"]

[deploy]
startCommand = "bun apps/stripe-webhook-handler/src/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
